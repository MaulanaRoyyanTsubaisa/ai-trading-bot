services:
  app:
    build: .
    env_file: .env
    restart: unless-stopped
    ports:
      - "127.0.0.1:${DASHBOARD_PORT:-8790}:8000"
    volumes:
      - signal-data:/app/data

volumes:
  signal-data:
